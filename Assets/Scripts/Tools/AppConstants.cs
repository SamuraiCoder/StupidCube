using UnityEngine;

namespace Constants
{
    //This defines all the game states that app will handle.
    public enum GameState
    {
        START,
        GAME,
        PAUSE,
        END,
    }

    public enum BridgeType
    {
        MOVING,
        ROTATING,
    }
}
